<% if current_user %>
<h4> Give us your thoughts!</h4>
  <%= form_for [@post, @post.comments.build], remote: true do |f| %>
      <%= f.label :body %>
      <%= f.text_field :body, rows: 2, class: 'form-control', placeholder: "Enter comment" %>
      <%= f.submit %>
  <% end %>
<% end %>
